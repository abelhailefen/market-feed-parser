schema_version = "1.3"
artifacts = []

[task]
name = "afterquery/market-feed-parser"
description = "Fix a Rust binary market-data feed parser that panics on truncated frames and silently drops messages when processing multiple packed binary records."
authors = [{ name = "Abel Hailemariam", email = "abelhailefen2@gmail.com"}]
keywords = ["rust", "parser", "binary-protocol", "compilers-parsers-language-internals", "debugging"]

[metadata]

[verifier]
timeout_sec = 600.0

[verifier.env]

[agent]
timeout_sec = 600.0

[environment]
network_mode = "no-network"
build_timeout_sec = 900.0
os = "linux"
mcp_servers = []

[environment.env]

[solution.env]